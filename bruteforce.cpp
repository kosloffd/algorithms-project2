#include "bruteforce.h"
#include "utility.h"

void changeslow(int amount, int *values, int numLines) {

    // int spaces = std::count_if(s.begin(), s.end(), [](unsigned char c){ return std::isspace(c); });

}